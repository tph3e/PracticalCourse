# Branching

João branching methods provide probability, attribute, attribute-sampling, predictive, and composite BPMN-valid next-activity selection. Runtime calls must return only activities supplied by the BPMN engine.
