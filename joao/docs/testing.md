# Testing

Run `PYTHONPATH=joao python3 -m pytest joao/tests`. Audit-specific tests are `test_full_method_audit_invariants.py` and `test_full_method_audit_branching.py`. Controlled audit outputs are under `joao/results/full_method_audit`.
